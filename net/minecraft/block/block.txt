Block
静态成员:
    所有的注册方块
    声音类型
实例成员:
    创造模式的分页类型
    材质名称
    唯一标识名称
    IIcon 图标
    材料类型
    包围盒边界 2x 2y 2z max min
    方块渲染数据: {
        透明度 光线不透明度 亮度值(自发光) 
    }
    游戏数据: {
        硬度 抗爆性 是否能长草 是否统计数据(挖掘计数器) 是否是容器(TileEntity) 滑行数值(冰)
    }

仅客户端方法:
    获取所有meta块

    获取混合亮度
    是否渲染一个面
    获取选择边界框(指向方块时渲染的黑框)
    随机更新(空)
    获取应在哪个通道渲染, 0固体 1透明
    获取基于块不透明度的默认环境光遮挡值